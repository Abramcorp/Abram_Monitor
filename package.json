{
  "name": "deal-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone deal status monitoring dashboard",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/analytics.js && node --check src/store.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
