{
  "name": "deal-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone deal status monitoring dashboard",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/analytics.js && node --check src/store.js && node --check src/postgresStore.js && node --check public/app.js"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "form-data": "^4.0.1",
    "googleapis": "^144.0.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
